import styled from "styled-components"

const Card = styled.div`
  border-radius: 8px;
  box-shadow: 0 16px 24px rgba(0, 0, 0, 0.25), 0 8px 8px rgba(0, 0, 0, 0.22);
  background-color: white;
`

export default Card
