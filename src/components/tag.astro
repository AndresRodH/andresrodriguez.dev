---
interface Props {
	tag: string;
}

const { tag } = Astro.props;
---

<span
	class="border-2 px-2 py-0.5 font-robot rounded-full text-sm font-semibold hover:bg-gray-200 transition-colors"
>
	#{tag}
</span>
